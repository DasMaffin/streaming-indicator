--Streaming is always preferred in the messages. If you enable streaming and recording you will show up as streaming only.<br>
!toggleStreaming --Toggles streaming indicator on/off<br>
!toggleRecording --Toggles recording indicator on/off
