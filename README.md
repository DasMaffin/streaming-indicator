--Streaming is always preferred in the messages. If you enable streaming and recording you will show up as streaming only.
!toggleStreaming --Toggles streaming indicator on/off
!toggleRecording --Toggles recording indicator on/off
